import { FontSize2 } from '@styled-icons/remix-editor/FontSize2';
import styled from 'styled-components';

import { SharedStylesIcon } from './SharedIconStyles';

export const FontSizeIcon = styled(FontSize2)`
  ${SharedStylesIcon}
`;
