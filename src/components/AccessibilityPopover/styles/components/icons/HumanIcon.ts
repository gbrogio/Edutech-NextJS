import { AccessibilityNew } from '@styled-icons/material-outlined';
import styled from 'styled-components';

import { SharedStylesIcon } from './SharedIconStyles';

export const HumanIcon = styled(AccessibilityNew)`
  ${SharedStylesIcon}
`;
