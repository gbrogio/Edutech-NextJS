import { css } from 'styled-components';

export const iconSize = '2rem';

export const SharedStylesIcon = css`
  width: ${iconSize};
  height: ${iconSize};
  color: var(--accessibility-popover-color-icon);
`;
